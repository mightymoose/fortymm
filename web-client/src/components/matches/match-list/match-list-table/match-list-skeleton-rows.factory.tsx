/** Props for `MatchListSkeletonRows` — the six-row shimmer placeholder shown
 * while the first page loads. It takes no props, so this builds the empty props
 * object. */
export function buildMatchListSkeletonRowsProps(
  overrides: Record<string, never> = {},
): Record<string, never> {
  return { ...overrides };
}
