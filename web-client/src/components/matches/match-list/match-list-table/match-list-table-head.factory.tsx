/** Props for `MatchListTableHead` — the static column header row. It takes no
 * props, so this builds the empty props object. */
export function buildMatchListTableHeadProps(
  overrides: Record<string, never> = {},
): Record<string, never> {
  return { ...overrides };
}
