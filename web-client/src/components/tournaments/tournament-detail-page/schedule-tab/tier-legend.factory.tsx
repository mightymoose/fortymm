/** `TierLegend` takes no props; the builder exists so the quartet's shape stays
 * uniform (and grows overrides the day the legend takes any). */
export function buildTierLegendProps(): Record<string, never> {
  return {}
}
